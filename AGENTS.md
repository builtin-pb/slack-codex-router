# Repo AGENTS

This repository is maintained around the `v2` router.

- `v2/` is the active implementation and the default target for code changes, tests, and verification.
- `legacy/v1/` is archived compatibility code and is no longer maintained.
- Unless a user explicitly asks for `legacy/v1`, treat requests for router work, bug fixes, and test expansion as `v2` work.
